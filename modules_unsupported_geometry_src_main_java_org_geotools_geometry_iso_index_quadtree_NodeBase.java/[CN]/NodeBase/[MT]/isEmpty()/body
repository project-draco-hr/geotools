{
  boolean isEmpty=true;
  if (!items.isEmpty())   isEmpty=false;
  for (int i=0; i < 4; i++) {
    if (subnode[i] != null) {
      if (!subnode[i].isEmpty())       isEmpty=false;
    }
  }
  return isEmpty;
}

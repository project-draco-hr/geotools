{
  this.id=ITEM_ID++;
  if (name == null || name.trim().length() == 0) {
    setName("Item_" + id);
  }
 else {
    setName(name);
  }
  this.hasBorder=border;
  if (hasBorder) {
    setBorder(BorderFactory.createEtchedBorder());
  }
  setOpaque(false);
  minHeight=getHeight();
}

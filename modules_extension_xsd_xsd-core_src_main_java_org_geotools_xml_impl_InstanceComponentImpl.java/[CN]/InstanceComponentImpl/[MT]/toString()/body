{
  StringBuffer buf=new StringBuffer();
  buf.append(name);
  buf.append(" ");
  buf.append(namespace);
  if (text != null) {
    buf.append("\n\t");
    buf.append(text);
  }
  return buf.toString();
}

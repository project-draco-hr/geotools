{
  if (runConfiguration.isAbsolute())   return fileBeingProcessed.getAbsolutePath();
  String path=fileBeingProcessed.getCanonicalPath();
  path=path.substring(runConfiguration.getRootMosaicDirectory().length());
  return path;
}

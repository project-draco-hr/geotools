{
  if (elements != null) {
    for (int i=0; i < elements.length; i++) {
      Element e=elements[i].findChildElement(name);
      if (e != null) {
        return e;
      }
    }
  }
  return null;
}

{
  if (value == null)   return "-";
  String result=null;
  for (  HTMLRenderer renderer : HTMLRenderers.getRenderers()) {
    if (renderer.canRender(value))     result=renderer.render(value);
  }
  if (result == null)   result=value.toString();
  return result.replace("\n","<br>");
}

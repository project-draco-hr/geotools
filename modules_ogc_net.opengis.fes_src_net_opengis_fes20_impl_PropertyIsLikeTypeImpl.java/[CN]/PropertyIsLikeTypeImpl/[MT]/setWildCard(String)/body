{
  String oldWildCard=wildCard;
  wildCard=newWildCard;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,Fes20Package.PROPERTY_IS_LIKE_TYPE__WILD_CARD,oldWildCard,wildCard));
}

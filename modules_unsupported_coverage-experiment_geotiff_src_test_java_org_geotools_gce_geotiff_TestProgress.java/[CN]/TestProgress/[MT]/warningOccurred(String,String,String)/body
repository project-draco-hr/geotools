{
  StringBuffer warn=new StringBuffer();
  if (file == null) {
    warn.append(file);
    warn.append(" ");
  }
  if (location == null) {
    warn.append("#");
    warn.append(location);
    warn.append(" ");
  }
  warn.append(warning);
  warnings.add(warn.toString());
}

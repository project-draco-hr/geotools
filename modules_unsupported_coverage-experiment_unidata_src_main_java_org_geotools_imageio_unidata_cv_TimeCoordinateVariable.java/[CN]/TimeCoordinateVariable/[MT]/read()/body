{
  return new AbstractList<Date>(){
    @Override public Date get(    int index){
      return timeBuilder.buildTime(index);
    }
    @Override public int size(){
      return timeBuilder.getNumTimes();
    }
  }
;
}

{
  final Writer table;
  try {
    table=new TableWriter(null," ");
    format(table,hints,"  ",done);
  }
 catch (  IOException e) {
    throw new AssertionError(e);
  }
  return table.toString();
}

{
  if (a.length < m_values.length)   a=(Object[])Array.newInstance(a.getClass().getComponentType(),m_values.length);
  for (int i=0; i < m_values.length; i++) {
    a[i]=m_values[i];
  }
  if (a.length > m_values.length)   a[m_values.length]=null;
  return (a);
}

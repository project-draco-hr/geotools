{
  if (i > 1 && compare(i,i / 2) < 0) {
    return (moveUp(i));
  }
  if ((2 * i <= size() && compare(i,2) > 0) || (2 * i + 1 <= size() && compare(i,2 * i + 1) > 0)) {
    return (moveDown(i));
  }
  return (i);
}

{
  String r="";
  Iterator i=errors.keySet().iterator();
  if (i.hasNext()) {
    while (i.hasNext()) {
      SimpleFeature f=(SimpleFeature)i.next();
      String msg=(String)errors.get(f);
      r+=(f.getID() + " " + msg+ "\n");
    }
  }
 else {
    r="PASSED";
  }
  return r + "\n";
}

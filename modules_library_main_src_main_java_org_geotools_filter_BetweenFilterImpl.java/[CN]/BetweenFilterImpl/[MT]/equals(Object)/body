{
  if (oFilter.getClass() == this.getClass()) {
    BetweenFilterImpl bFilter=(BetweenFilterImpl)oFilter;
    return (expression1 == bFilter.getExpression1() || (expression1 != null && expression1.equals(bFilter.getExpression1()))) && (expression2 == bFilter.getExpression2() || (expression2 != null && expression2.equals(bFilter.getExpression2()))) && (expression1 == bFilter.getExpression() || (middleValue != null && middleValue.equals(bFilter.getExpression())));
  }
 else {
    return false;
  }
}

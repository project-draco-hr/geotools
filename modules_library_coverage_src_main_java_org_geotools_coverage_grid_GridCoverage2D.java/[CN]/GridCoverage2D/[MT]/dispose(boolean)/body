{
  if (views != null) {
    if (views.dispose(force).contains(this)) {
      return false;
    }
    views=null;
  }
 else   if (!disposeImage(force)) {
    return false;
  }
  return super.dispose(force);
}

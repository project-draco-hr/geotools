{
  if (crs == null) {
    return 2;
  }
  int dimension=crs.getCoordinateSystem().getDimension();
  if (dimension == 2) {
    return TWO_DIMENSIONIAL;
  }
 else   if (dimension == 3) {
    return THREE_DIMENSIONIAL;
  }
 else {
    return -1;
  }
}

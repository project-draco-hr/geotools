{
  Toolbox toolBox=new Toolbox();
  boolean retval=false;
  try {
    retval=toolBox.parse(args);
  }
 catch (  IOException e) {
    e.printStackTrace();
    retval=false;
  }
  if (retval)   System.exit(0);
 else   System.exit(1);
}

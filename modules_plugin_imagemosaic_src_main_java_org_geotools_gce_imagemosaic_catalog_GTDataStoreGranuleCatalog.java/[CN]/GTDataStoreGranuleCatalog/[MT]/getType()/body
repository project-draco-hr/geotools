{
  final Lock lock=rwLock.readLock();
  try {
    lock.lock();
    checkStore();
    if (typeName == null) {
      return null;
    }
    return tileIndexStore.getSchema(typeName);
  }
  finally {
    lock.unlock();
  }
}

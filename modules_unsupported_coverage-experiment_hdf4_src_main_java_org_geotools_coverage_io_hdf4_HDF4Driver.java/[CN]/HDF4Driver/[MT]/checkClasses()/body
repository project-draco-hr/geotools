{
  boolean available=true;
  try {
    Class.forName("javax.media.jai.JAI");
    Class.forName("com.sun.media.jai.operator.ImageReadDescriptor");
    Class.forName("org.geotools.imageio.hdf4.HDF4SpatioTemporalImageReaderSpi");
    if (available)     if (LOGGER.isLoggable(Level.FINE))     LOGGER.fine("HDF4Driver is availaible.");
  }
 catch (  ClassNotFoundException cnf) {
    if (LOGGER.isLoggable(Level.FINE))     LOGGER.fine("HDF4Driver is not availaible.");
    available=false;
  }
  return available;
}

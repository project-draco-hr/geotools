{
  if (b1.colors == 1) {
    if (b2.colors == 1) {
      return compareLong(b2.sum,b1.sum);
    }
 else {
      return 1;
    }
  }
 else   if (b2.colors == 1) {
    return -1;
  }
  return compareLong(b2.sum,b1.sum);
}

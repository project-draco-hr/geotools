{
  final int rank=var.getRank();
  if (rank > 2) {
    if (rank == 3) {
      return (imageIndex - range.first());
    }
 else {
      return (int)Math.ceil((imageIndex - range.first()) / UnidataUtilities.getZDimensionLength(var));
    }
  }
  return -1;
}

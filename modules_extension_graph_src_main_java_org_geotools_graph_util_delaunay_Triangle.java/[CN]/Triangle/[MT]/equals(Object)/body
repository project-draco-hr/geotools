{
  boolean ret;
  if (o instanceof Triangle) {
    ret=((this.containsEdge(((Triangle)o).getEdges()[0])) && (this.containsEdge(((Triangle)o).getEdges()[1])) && (this.containsEdge(((Triangle)o).getEdges()[2])));
  }
 else {
    ret=false;
  }
  return ret;
}

{
  if (source != null) {
    for (int i=0; i < changers.length; i++) {
      if (changers[i].source.isAssignableFrom(source)) {
        return changers[i].target;
      }
    }
  }
  return source;
}

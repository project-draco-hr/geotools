{
  return (cond.lowerInclusive ? (cond.lowerBound.compareTo(date) <= 0) : (cond.lowerBound.compareTo(date) < 0)) && (cond.upperInclusive ? (cond.upperBound.compareTo(date) >= 0) : (cond.upperBound.compareTo(date) > 0));
}

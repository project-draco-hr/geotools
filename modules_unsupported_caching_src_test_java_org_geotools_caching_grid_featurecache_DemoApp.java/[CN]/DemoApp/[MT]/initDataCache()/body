{
  try {
    cache=new GridFeatureCache(ds.getFeatureSource(ds.getTypeNames()[0]),100,100,MemoryStorage.createInstance());
  }
 catch (  FeatureCacheException e) {
    e.printStackTrace();
  }
catch (  IOException e) {
    e.printStackTrace();
  }
}

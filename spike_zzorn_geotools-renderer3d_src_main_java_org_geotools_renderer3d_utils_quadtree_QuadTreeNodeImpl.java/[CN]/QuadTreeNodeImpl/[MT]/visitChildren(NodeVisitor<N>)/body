{
  checkNodeIsAttached();
  if (myChildren != null) {
    for (    QuadTreeNode<N> child : myChildren) {
      if (!visitor.visitNode(child)) {
        return false;
      }
    }
  }
  return true;
}

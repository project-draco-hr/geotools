{
  if (mapContent != null && !mapContent.layers().isEmpty() && !mapContent.getViewport().isEmpty() && acceptRepaintRequests.get()) {
    if (mapContent != null) {
      getRenderingExecutor().submit(mapContent,getOperands(recreate),this);
    }
  }
}

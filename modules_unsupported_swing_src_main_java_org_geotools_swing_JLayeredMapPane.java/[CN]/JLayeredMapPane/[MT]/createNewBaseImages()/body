{
  Rectangle r=getVisibleRect();
  for (  Layer layer : mapContent.layers()) {
    BufferedImage image=GraphicsEnvironment.getLocalGraphicsEnvironment().getDefaultScreenDevice().getDefaultConfiguration().createCompatibleImage(r.width,r.height,Transparency.TRANSLUCENT);
    baseImages.put(layer,image);
  }
}

{
  if (baseImages.isEmpty()) {
    return false;
  }
  for (  Layer layer : mapContent.layers()) {
    if (layer.isVisible() && baseImages.get(layer) == null) {
      return false;
    }
  }
  return true;
}

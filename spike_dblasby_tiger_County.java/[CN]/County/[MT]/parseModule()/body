{
  Pattern p=Pattern.compile("TGR([0123456789][0123456789])([0123456789][0123456789][0123456789])");
  Matcher m=p.matcher(MODULE);
  if (m.matches()) {
    state=m.group(1);
    county=m.group(2);
  }
 else   throw new Exception("cannot parse module:" + MODULE);
}

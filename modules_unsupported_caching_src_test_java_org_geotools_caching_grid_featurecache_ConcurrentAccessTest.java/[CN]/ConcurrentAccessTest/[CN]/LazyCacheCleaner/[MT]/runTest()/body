{
  int count=0;
  while (count < repeat) {
    Thread.sleep(rand.nextInt(maxdelay));
    if (Thread.interrupted()) {
      throw new InterruptedException();
    }
    grid.clear();
    count++;
  }
}

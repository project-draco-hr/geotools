{
  if (isConstant(ex)) {
    Double result=ex.evaluate(null,Double.class);
    if (result != null) {
      return (int)Math.ceil(result);
    }
 else {
      return -1;
    }
  }
 else {
    return -1;
  }
}

{
  assertTrue(arrayReader.hasNext());
  arrayReader.next();
  assertTrue(arrayReader.hasNext());
  arrayReader.next();
  assertTrue(arrayReader.hasNext());
  arrayReader.next();
  assertTrue(arrayReader.hasNext());
  arrayReader.next();
  assertTrue(arrayReader.hasNext());
  arrayReader.next();
  assertTrue(arrayReader.hasNext());
  arrayReader.next();
  assertFalse(arrayReader.hasNext());
}

{
  boolean result1=GpxUtils.isNorthLatitude(52.3662);
  boolean result2=GpxUtils.isNorthLatitude(-45.3662);
  if (result1 == false) {
    fail("Method did not properly recognize a latitide in the " + "northern hemisphere.");
  }
  if (result2 == true) {
    fail("Method recognized a latitide in the " + "southern hemisphere as being in the northern " + "hemisphere.");
  }
}

{
  if (newItems.length > 0) {
    int index0=items.size();
    for (    T item : newItems) {
      items.add(item);
    }
    if (notify) {
      fireIntervalAdded(this,index0,index0 + newItems.length - 1);
    }
  }
}

{
  if (unset) {
    return null;
  }
  Map<String,Expression> params=new HashMap<String,Expression>();
  for (  Entry<String,ChildExpressionBuilder<ExtensionSymbolizerBuilder<P>>> entry : parameters.entrySet()) {
    params.put(entry.getKey(),entry.getValue().build());
  }
  ExtensionSymbolizer symbolizer=sf.extensionSymbolizer(name,geometry,description,unit,extensionName,params);
  return symbolizer;
}

{
  if (geom != null) {
    String id=GML2EncodingUtils.getID(geom);
    if (id != null && idSet.idExists(id)) {
      makeEmpty=true;
    }
 else     if (id != null) {
      idSet.add(id);
    }
  }
  return;
}

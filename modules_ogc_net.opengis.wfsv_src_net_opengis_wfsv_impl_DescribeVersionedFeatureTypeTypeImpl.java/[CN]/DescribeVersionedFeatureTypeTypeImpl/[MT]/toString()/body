{
  if (eIsProxy())   return super.toString();
  StringBuffer result=new StringBuffer(super.toString());
  result.append(" (versioned: ");
  if (versionedESet)   result.append(versioned);
 else   result.append("<unset>");
  result.append(')');
  return result.toString();
}

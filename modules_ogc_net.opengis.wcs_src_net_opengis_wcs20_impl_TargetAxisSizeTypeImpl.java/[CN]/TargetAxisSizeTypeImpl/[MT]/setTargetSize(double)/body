{
  double oldTargetSize=targetSize;
  targetSize=newTargetSize;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,Wcs20Package.TARGET_AXIS_SIZE_TYPE__TARGET_SIZE,oldTargetSize,targetSize));
}

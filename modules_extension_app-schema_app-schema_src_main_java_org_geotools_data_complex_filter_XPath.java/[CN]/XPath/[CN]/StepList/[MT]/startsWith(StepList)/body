{
  if (other.size() > this.size()) {
    return false;
  }
  for (int i=0; i < other.size(); i++) {
    if (!this.get(i).equalsIgnoreIndex(other.get(i))) {
      return false;
    }
  }
  return true;
}

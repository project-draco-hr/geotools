{
  Document dom=encode(GeometryOperand.Envelope,new QName(OGC.NAMESPACE,"GeometryOperand"),OGC.GeometryOperandType);
  assertEquals("gml:Envelope",dom.getDocumentElement().getFirstChild().getNodeValue());
}

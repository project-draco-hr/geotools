{
  super(preFix(attributes,schema),checkSchema(schema),SimpleFeatureBuilder.createDefaultFeatureIdentifier(featureID));
  constructing=true;
  List<Object> values=toValues(attributes);
  setAttributes(values);
  constructing=false;
}

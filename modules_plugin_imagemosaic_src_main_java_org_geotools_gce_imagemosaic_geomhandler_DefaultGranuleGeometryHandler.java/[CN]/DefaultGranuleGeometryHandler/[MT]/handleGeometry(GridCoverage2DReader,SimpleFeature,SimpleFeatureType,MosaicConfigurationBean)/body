{
  Envelope coverageEnvelope=inputReader.getOriginalEnvelope();
  feature.setAttribute(indexSchema.getGeometryDescriptor().getLocalName(),GEOM_FACTORY.toGeometry(new ReferencedEnvelope(coverageEnvelope)));
}

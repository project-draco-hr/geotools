{
  boolean available=true;
  try {
    Class.forName("javax.media.jai.JAI");
    Class.forName("com.sun.media.jai.operator.ImageReadDescriptor");
    Class.forName("org.geotools.imageio.grib1.GRIB1SpatioTemporalImageReaderSpi");
    if (available)     if (LOGGER.isLoggable(Level.FINE))     LOGGER.fine("GRIB1Driver is availaible.");
  }
 catch (  ClassNotFoundException cnf) {
    if (LOGGER.isLoggable(Level.FINE))     LOGGER.fine("GRIB1Driver is not availaible.");
    available=false;
  }
  return available;
}

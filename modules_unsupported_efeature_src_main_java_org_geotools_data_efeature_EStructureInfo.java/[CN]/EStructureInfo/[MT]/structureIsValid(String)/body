{
  isValid=true;
  StackTraceElement trace=EFeatureUtils.getStackTraceElement(0,1);
  String msg="Structure is valid: " + trace.getClassName() + "["+ context+ "]#"+ trace.getMethodName()+ "()";
  return success(msg);
}

{
  @SuppressWarnings("unchecked") List<String> keywords=eType.getKeywords();
  Set<String> ret;
  if (keywords == null) {
    ret=Collections.emptySet();
  }
 else {
    ret=new HashSet<String>(keywords);
    ret.remove(null);
  }
  return ret;
}

{
  WKTMarkFactory wmf=new WKTMarkFactory();
  try {
    this.exp=ff.literal(WKTMarkFactory.WKT_PREFIX + "POLYGON((0.25 0.25, 0.5 0.75, 0.75 0.25, 0.25 0.25))");
    wmf.getShape(null,this.exp,this.feature);
  }
 catch (  Exception e) {
    assertTrue(false);
    return;
  }
  assertTrue(true);
}

{
  WKTMarkFactory wmf=new WKTMarkFactory();
  try {
    this.exp=ff.literal("wkt://POLYGON((0.25 0.25, 0.5 0.75, 0.75 0.25,     ))");
    wmf.getShape(null,this.exp,this.feature);
  }
 catch (  Exception e) {
    assertTrue(true);
    return;
  }
  assertTrue(false);
}

{
  if (datum.getSelectedChoice() != Datum.VERTICAL_DATUM)   return datum.getChild().getString(SpatioTemporalMetadataFormat.MD_DTM_REALIZATIONEPOCH);
 else   throw new IllegalArgumentException("Invalid selection for Temporal Datum type");
}

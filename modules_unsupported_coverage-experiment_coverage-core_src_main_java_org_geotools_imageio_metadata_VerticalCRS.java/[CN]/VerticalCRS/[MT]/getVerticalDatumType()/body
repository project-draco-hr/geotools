{
  if (datum.getSelectedChoice() == Datum.VERTICAL_DATUM)   return datum.getChild().getString(SpatioTemporalMetadataFormat.MD_DTM_VD_TYPE);
 else   throw new IllegalArgumentException("Could not get Vertical Datum Type for non-Vertical Datum type");
}

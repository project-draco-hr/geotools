{
  if (datum.getSelectedChoice() != Datum.VERTICAL_DATUM)   datum.getChild().setString(SpatioTemporalMetadataFormat.MD_DTM_REALIZATIONEPOCH,value);
 else   throw new IllegalArgumentException("Could not set Realization Epoch for Temporal Datum type");
}

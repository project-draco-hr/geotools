{
  IIOMetadataNode node=getTiffField(GeoTIFFTagSet.TAG_MODEL_TIE_POINT);
  if (node == null)   return false;
  final double tiePoints[]=getTiffDoubles(node);
  if (tiePoints == null || tiePoints.length <= 0)   return false;
  return true;
}

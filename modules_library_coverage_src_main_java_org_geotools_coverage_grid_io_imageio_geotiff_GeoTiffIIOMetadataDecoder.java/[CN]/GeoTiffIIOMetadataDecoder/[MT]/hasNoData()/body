{
  final IIOMetadataNode noDataNode=getTiffField(GeoTiffConstants.TIFFTAG_NODATA);
  if (noDataNode == null)   return false;
  final String noData=getTiffAscii(noDataNode);
  if (noData == null || noData.trim().length() == 0)   return false;
  return true;
}

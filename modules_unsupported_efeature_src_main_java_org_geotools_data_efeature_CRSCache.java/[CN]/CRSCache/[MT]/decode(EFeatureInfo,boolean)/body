{
  try {
    eInfo.crs=decode(eInfo.srid);
    return eInfo.crs;
  }
 catch (  Exception e) {
    if (!tryDefault) {
      throw e;
    }
  }
  eInfo.srid=EFeatureConstants.DEFAULT_SRID;
  return decode(eInfo,false);
}

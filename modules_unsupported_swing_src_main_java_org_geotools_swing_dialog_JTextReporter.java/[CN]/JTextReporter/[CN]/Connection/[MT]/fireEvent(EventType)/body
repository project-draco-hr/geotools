{
  for (  TextReporterListener listener : listeners) {
switch (eventType) {
case DIALOG_CLOSED:
      listener.onReporterClosed();
    break;
case TEXT_UPDATED:
  listener.onReporterUpdated();
break;
}
}
}

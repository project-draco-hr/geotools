{
  for (  File file : dir.listFiles()) {
    if (file.isDirectory()) {
      File found=searchDirectory(file,fileName);
      if (found != null) {
        return found;
      }
    }
    if (fileName.equals(file.getName())) {
      return file;
    }
  }
  return null;
}

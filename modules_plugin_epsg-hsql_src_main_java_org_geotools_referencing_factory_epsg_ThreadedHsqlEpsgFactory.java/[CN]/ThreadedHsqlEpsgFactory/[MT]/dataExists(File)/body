{
  return new File(directory,MARKER_FILE).exists() && new File(directory,DATABASE_NAME + ".data").exists() && new File(directory,DATABASE_NAME + ".properties").exists()&& new File(directory,DATABASE_NAME + ".script").exists();
}

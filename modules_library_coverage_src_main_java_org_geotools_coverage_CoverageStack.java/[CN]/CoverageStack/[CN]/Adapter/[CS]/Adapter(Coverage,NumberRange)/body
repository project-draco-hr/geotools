{
  this.coverage=coverage;
  this.range=range;
  if (getClass() == Adapter.class) {
    if (coverage == null) {
      throw new IllegalArgumentException(Errors.format(ErrorKeys.NULL_ARGUMENT_$1,"coverage"));
    }
  }
}

{
  createReporter();
  reporter.append(layerName);
  reporter.append("\n");
  int k=1;
  for (  Number value : bandValues) {
    reporter.append(String.format("  Band %d: %s\n",k++,value.toString()));
  }
  reporter.append("\n");
}

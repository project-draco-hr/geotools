{
  String oldRecordSchema=recordSchema;
  recordSchema=newRecordSchema;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,Csw20Package.SEARCH_RESULTS_TYPE__RECORD_SCHEMA,oldRecordSchema,recordSchema));
}

{
  BigInteger oldNextRecord=nextRecord;
  nextRecord=newNextRecord;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,Csw20Package.SEARCH_RESULTS_TYPE__NEXT_RECORD,oldNextRecord,nextRecord));
}

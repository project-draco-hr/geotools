{
  super(elements.get(0).getCoordinateReferenceSystem());
  if (elements.isEmpty())   throw new IllegalArgumentException("Array of Elements is empty. At least one element expected.");
  this.elements=elements;
  this.subComplex=this.createBoundary();
  this.superComplex=null;
}

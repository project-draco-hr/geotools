{
  String oldVersion=version;
  version=newVersion;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,WfsvPackage.VERSIONED_FEATURE_COLLECTION_TYPE__VERSION,oldVersion,version));
}

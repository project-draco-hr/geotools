{
  String message=getErrorResources().getString(ErrorKeys.BAD_LINE_IN_FILE_$2,name,input.getLineNumber());
  if (cause != null) {
    message+=cause.getLocalizedMessage();
  }
  return new IIOException(message,cause);
}

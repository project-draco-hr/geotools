{
  if (model.getSize() > 0) {
    if (model.getSize() == 1) {
      selectCRS(0);
    }
 else {
      int index=listBox.getSelectedIndex();
      if (index >= 0) {
        selectCRS(index);
      }
    }
  }
  setVisible(false);
}

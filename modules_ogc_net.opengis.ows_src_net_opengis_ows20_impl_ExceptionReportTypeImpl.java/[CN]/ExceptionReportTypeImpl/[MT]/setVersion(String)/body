{
  String oldVersion=version;
  version=newVersion;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,Ows20Package.EXCEPTION_REPORT_TYPE__VERSION,oldVersion,version));
}

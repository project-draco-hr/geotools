{
  final BaseCoverageRequest request=new BaseCoverageRequest(params,this);
  GridCoverage2D gc=((GridCoverage2D)(requestCoverage(request).getGridCoverage()));
  if (validRange != null)   return gc.view(ViewType.GEOPHYSICS);
  return gc;
}

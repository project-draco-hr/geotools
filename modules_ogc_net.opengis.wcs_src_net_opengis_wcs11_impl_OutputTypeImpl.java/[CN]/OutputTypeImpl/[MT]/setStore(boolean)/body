{
  boolean oldStore=store;
  store=newStore;
  boolean oldStoreESet=storeESet;
  storeESet=true;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,Wcs111Package.OUTPUT_TYPE__STORE,oldStore,store,!oldStoreESet));
}

{
  if (next == null) {
    while (it.hasNext()) {
      SimpleFeature f=it.next();
      if (f.getProperty(descriptor.getName()).getValue() != null) {
        next=f;
        break;
      }
    }
  }
  return next != null;
}

{
  List<OrientableCurve> tmpCurves=this.getGenerators();
  if (tmpCurves.isEmpty())   return 0.0;
  double length=0.0;
  for (int i=1; i < tmpCurves.size(); ++i) {
    length=DoubleOperation.add(length,((CurveImpl)tmpCurves.get(i)).length());
  }
  return length;
}

{
  if (unset) {
    return null;
  }
  Font[] fonts=null;
  TextSymbolizer ts=sf.createTextSymbolizer(fill.build(),fonts,halo.build(),label.build(),labelPlacement,geometry);
  reset();
  return ts;
}

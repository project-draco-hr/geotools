{
  sql.append("CAST(");
  encodeColumnName(prefix,gatt.getLocalName(),sql);
  sql.append(".STSrid as VARCHAR)");
  sql.append(" + ':' + ");
  encodeColumnName(prefix,gatt.getLocalName(),sql);
  sql.append(".STAsText()");
}

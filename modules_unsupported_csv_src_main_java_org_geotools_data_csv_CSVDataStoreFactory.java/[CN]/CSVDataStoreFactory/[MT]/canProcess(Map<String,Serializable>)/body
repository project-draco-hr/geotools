{
  try {
    File file=(File)FILE_PARAM.lookUp(params);
    if (file != null) {
      return file.getPath().endsWith(".csv") || file.getPath().endsWith("CSV");
    }
  }
 catch (  IOException e) {
  }
  return false;
}

{
  if (productIndex > nProducts)   throw new ArrayIndexOutOfBoundsException("Specified product index is out of range");
 else   productList.set(productIndex,product);
  return productList.get(productIndex);
}

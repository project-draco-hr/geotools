{
  for (  ResourceIdentifierType rit : getResourceIdentifier()) {
    if (rit.getName().getNamespaceURI().startsWith("http://www.opengis.net/cat/csw/") && rit.getName().getLocalPart().equals("RecordId")) {
      return true;
    }
  }
  return false;
}

{
  Document dom=encode(FilterMockData.idCapabilities(),new QName(OGC.NAMESPACE,"IdCapabilities"),OGC.Id_CapabilitiesType);
  assertNotNull(getElementsByQName(dom,OGC.FID));
  assertNotNull(getElementsByQName(dom,OGC.EID));
}

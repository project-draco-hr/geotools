{
  this.entries=new ConcurrentHashMap<Name,ContentEntry>();
  this.LOGGER=org.geotools.util.logging.Logging.getLogger(getClass().getPackage().getName());
  setFilterFactory(CommonFactoryFinder.getFilterFactory());
}

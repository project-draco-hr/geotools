{
  for (int i=0, n=n(); i < n; ++i) {
    if (point[i] == n0 && point[(i + 1) % n] == n1) {
      return 1;
    }
 else     if (point[i] == n1 && point[(i + 1) % n] == n0) {
      return -1;
    }
  }
  return 0;
}

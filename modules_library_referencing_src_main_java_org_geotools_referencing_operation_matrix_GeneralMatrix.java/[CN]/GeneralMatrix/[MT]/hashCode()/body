{
  final int prime=31;
  int result=1;
  if (mat == null) {
    return prime * result;
  }
  result=prime * result + mat.numRows;
  result=prime * result + mat.numCols;
  result=prime * result + (int)mat.data[0];
  return result;
}

{
  if (protocol == null) {
    this.preferredMethod=PreferredHttpMethod.AUTO;
  }
 else {
    this.preferredMethod=protocol.booleanValue() ? PreferredHttpMethod.HTTP_POST : PreferredHttpMethod.HTTP_GET;
  }
}

{
  Iterator<SimpleFeature> it=list.iterator();
  while (it.hasNext()) {
    if (feat.equals(it.next()))     return true;
  }
  LOGGER.info("Could not match feature to list: " + feat);
  return false;
}

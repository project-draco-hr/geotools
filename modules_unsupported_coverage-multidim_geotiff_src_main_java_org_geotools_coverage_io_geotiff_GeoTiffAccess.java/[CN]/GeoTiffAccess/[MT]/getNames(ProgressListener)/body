{
  if (listener == null)   listener=new NullProgressListener();
  listener.started();
  try {
    return Collections.unmodifiableList(new ArrayList<Name>(coverageInfo.keySet()));
  }
  finally {
    listener.complete();
  }
}

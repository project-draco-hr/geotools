{
  final RTree tree;
  try {
    tree=getTree();
  }
 catch (  IOException e) {
    return super.toString();
  }
  final String string;
  try {
    string=OptionalDependencies.toString(tree.root);
  }
  finally {
    release(tree);
  }
  return string;
}

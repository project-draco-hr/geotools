{
  int length=roleType.length();
  boolean result=length >= 1;
  if (!result && diagnostics != null)   reportMinLengthViolation(XlinkPackage.Literals.ROLE_TYPE,roleType,length,1,diagnostics,context);
  return result;
}

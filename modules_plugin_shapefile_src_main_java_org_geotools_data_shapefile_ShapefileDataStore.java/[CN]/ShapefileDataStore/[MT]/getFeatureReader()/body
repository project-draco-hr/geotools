{
  try {
    return createFeatureReader(getSchema().getTypeName(),getAttributesReader(true,null,null),schema);
  }
 catch (  SchemaException se) {
    throw new DataSourceException("Error creating schema",se);
  }
}

{
  double tmp;
  for (int i=0; i < array.length; i++) {
    tmp=array[i];
    for (int j=i + 1; j < array.length; j++) {
      if (array[j] < tmp) {
        array[i]=array[j];
        array[j]=tmp;
        tmp=array[i];
      }
    }
  }
}

{
  return (p0.getX() < p1.getX() ? intersects(p0.getX(),p1.getX(),p.getX()) : intersects(p1.getX(),p0.getX(),p.getX())) || (p0.getY() < p1.getY() ? intersects(p0.getY(),p1.getY(),p.getY()) : intersects(p1.getY(),p0.getY(),p.getY()));
}

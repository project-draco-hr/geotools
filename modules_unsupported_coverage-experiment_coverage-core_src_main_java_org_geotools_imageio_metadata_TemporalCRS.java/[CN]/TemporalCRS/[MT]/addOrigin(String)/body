{
  if (datum.getSelectedChoice() == Datum.TEMPORAL_DATUM)   datum.getChild().setString(SpatioTemporalMetadataFormat.MD_DTM_TD_ORIGIN,value);
 else   throw new IllegalArgumentException("Could not set Origin for non-Temporal Datum type");
}

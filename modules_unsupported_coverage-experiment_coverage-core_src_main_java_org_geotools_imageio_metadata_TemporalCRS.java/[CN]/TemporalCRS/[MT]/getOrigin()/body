{
  if (datum.getSelectedChoice() == Datum.TEMPORAL_DATUM)   return datum.getChild().getString(SpatioTemporalMetadataFormat.MD_DTM_TD_ORIGIN);
 else   throw new IllegalArgumentException("Could not get Origin for non-Temporal Datum type");
}

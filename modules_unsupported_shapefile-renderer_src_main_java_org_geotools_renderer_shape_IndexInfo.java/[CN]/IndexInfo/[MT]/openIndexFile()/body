{
  if (shpFiles.get(SHX) == null || (shpFiles.isLocal() && !shpFiles.exists(SHX))) {
    return null;
  }
  try {
    return new IndexFile(shpFiles,false);
  }
 catch (  Exception e) {
    return null;
  }
}

{
  int count=0;
  if (subtype) {
    for (    Class<?> it : typeMap.values()) {
      if (it.isAssignableFrom(type))       count++;
    }
  }
 else {
    for (    Class<?> it : typeMap.values()) {
      if (type.isAssignableFrom(it))       count++;
    }
  }
  return count;
}

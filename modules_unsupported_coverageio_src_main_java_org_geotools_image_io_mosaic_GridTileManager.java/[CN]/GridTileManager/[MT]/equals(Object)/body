{
  if (object != null && object.getClass().equals(getClass())) {
    final GridTileManager that=(GridTileManager)object;
    return count == that.count && Utilities.equals(region,that.region) && Utilities.equals(root,that.root);
  }
  return false;
}

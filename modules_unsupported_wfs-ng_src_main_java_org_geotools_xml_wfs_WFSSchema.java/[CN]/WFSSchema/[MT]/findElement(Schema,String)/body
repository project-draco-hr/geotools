{
  if ((name == null) || "".equals(name)) {
    return null;
  }
  Element[] elems=s.getElements();
  if (elems == null) {
    return null;
  }
  for (int i=0; i < elems.length; i++)   if (name.equals(elems[i].getName())) {
    return elems[i];
  }
  return null;
}

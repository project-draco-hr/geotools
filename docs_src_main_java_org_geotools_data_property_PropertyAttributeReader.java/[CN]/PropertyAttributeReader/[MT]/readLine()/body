{
  while (true) {
    String line=reader.readLine();
    if (line == null) {
      return null;
    }
    if (line.startsWith("#") || line.startsWith("!")) {
      continue;
    }
 else {
      return line;
    }
  }
}

{
  String[] testVals={null,""};
  for (int i=0; i < testVals.length; i++) {
    varRowField=new RowField(testVals[i],DATA_TEXT);
    assertEquals("Testing RowField for String value: " + testVals[i],testVals[i],varRowField.toString());
  }
}

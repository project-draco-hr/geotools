{
  if (selection == null || (selectedFactory=findProcessFactoryByTitle(selection.toString())) == null) {
    descLabel.setText(defaultDesc);
    selectedFactory=null;
    updateNavButtons();
    return;
  }
  descLabel.setText(selectedFactory.getDescription().toString());
  updateNavButtons();
}

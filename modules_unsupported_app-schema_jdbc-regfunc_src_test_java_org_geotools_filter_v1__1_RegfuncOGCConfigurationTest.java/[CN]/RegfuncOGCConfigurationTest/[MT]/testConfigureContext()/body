{
  OGCConfiguration configuration=new RegfuncOGCConfiguration();
  MutablePicoContainer container=new DefaultPicoContainer();
  configuration.configureContext(container);
  assertEquals(RegfuncFilterFactoryImpl.class,container.getComponentInstanceOfType(FilterFactory.class).getClass());
}

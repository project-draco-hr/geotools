{
  final T piece=findDomainElement(value);
  if (piece == null) {
    if (hasDefaultValue())     return getDefaultValue();
    throw new TransformException(Errors.format(ErrorKeys.TRANSFORM_EVALUATION_$1,new Double(value)));
  }
  return piece.transform(value);
}

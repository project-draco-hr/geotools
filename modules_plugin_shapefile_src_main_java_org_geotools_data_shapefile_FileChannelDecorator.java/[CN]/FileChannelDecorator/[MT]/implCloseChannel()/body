{
  try {
    wrapped.close();
  }
  finally {
    if (!closed) {
      closed=true;
      if (reader != null) {
        shapefileFiles.unlockRead(url,reader);
      }
 else {
        shapefileFiles.unlockWrite(url,writer);
      }
    }
  }
}

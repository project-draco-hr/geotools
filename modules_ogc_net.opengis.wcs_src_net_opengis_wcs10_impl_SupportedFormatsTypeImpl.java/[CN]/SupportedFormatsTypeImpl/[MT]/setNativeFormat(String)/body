{
  String oldNativeFormat=nativeFormat;
  nativeFormat=newNativeFormat;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,Wcs10Package.SUPPORTED_FORMATS_TYPE__NATIVE_FORMAT,oldNativeFormat,nativeFormat));
}

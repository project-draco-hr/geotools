{
  if (cursorTool != null) {
    listeners.remove(cursorTool);
  }
  cursorTool=tool;
  if (cursorTool != null) {
    cursorTool.setMapPane(mapPane);
    return listeners.add(tool);
  }
 else {
    return true;
  }
}

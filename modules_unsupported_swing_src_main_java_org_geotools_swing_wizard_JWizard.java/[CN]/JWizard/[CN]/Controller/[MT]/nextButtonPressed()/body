{
  if (current == null) {
    close(ERROR);
    return;
  }
  if (!current.isValid()) {
    syncButtonsToPage();
    return;
  }
  String nextId=current.getNextPageIdentifier();
  setCurrentPanel(nextId);
}

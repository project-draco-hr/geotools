{
  String oldElectronicMailAddress=electronicMailAddress;
  electronicMailAddress=newElectronicMailAddress;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,Ows20Package.ADDRESS_TYPE__ELECTRONIC_MAIL_ADDRESS,oldElectronicMailAddress,electronicMailAddress));
}

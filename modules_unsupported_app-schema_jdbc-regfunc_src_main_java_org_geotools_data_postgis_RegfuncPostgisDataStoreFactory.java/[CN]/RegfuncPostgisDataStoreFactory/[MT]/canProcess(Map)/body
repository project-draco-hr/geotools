{
  if (params != null && params.get(DBTYPE.key) != null && ((String)params.get(DBTYPE.key)).equalsIgnoreCase((String)DBTYPE.sample)) {
    return super.canProcess(rewriteParamsForSuper(params));
  }
 else {
    return false;
  }
}

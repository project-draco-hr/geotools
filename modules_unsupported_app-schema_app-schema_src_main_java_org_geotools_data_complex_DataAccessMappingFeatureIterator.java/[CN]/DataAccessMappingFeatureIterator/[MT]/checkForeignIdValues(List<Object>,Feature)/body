{
  if (foreignIds != null) {
    for (int i=0; i < foreignIds.size(); i++) {
      if (!foreignIds.get(i).evaluate(next).equals(foreignIdValues.get(i))) {
        return false;
      }
    }
  }
  return true;
}

{
  int prime=17;
  int hash=prime * (hash(serverName) + hash(portNumber) + hash(databaseName)+ hash(userName)+ hash(password)+ hash(minConnections)+ hash(maxConnections)+ hash(connTimeOut));
  return hash;
}

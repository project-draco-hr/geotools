{
  if (intersects(roi)) {
    if (tile != null) {
      tiles.add(tile);
    }
    TreeNode child=firstChildren;
    while (child != null) {
      child.intersecting(roi,tiles);
      child=child.nextSibling;
    }
  }
}

{
  final String appInfo=String.format("GeoFoo: foo mapping in real time %n" + "Version 0.0.1");
  SwingUtilities.invokeLater(new Runnable(){
    @Override public void run(){
      AboutDialog dialog=new AboutDialog("About",appInfo);
    }
  }
);
}

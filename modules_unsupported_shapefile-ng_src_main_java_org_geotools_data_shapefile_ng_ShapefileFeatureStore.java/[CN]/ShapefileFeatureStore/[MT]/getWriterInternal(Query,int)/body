{
  ShapefileFeatureReader reader=(ShapefileFeatureReader)delegate.getReaderInternal(query);
  return new ShapefileFeatureWriter(delegate.shpFiles,reader,getDataStore().getCharset(),getDataStore().getTimeZone());
}

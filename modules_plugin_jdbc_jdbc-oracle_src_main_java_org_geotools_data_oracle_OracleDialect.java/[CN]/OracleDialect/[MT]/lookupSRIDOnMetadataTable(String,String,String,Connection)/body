{
  if (geometryMetadataTable == null) {
    return null;
  }
  String metadataTableStatement="SELECT SRID FROM " + geometryMetadataTable + " WHERE F_TABLE_NAME = '"+ tableName+ "'"+ " AND F_GEOMETRY_COLUMN = '"+ columnName+ "'";
  if (schema != null && !"".equals(schema)) {
    metadataTableStatement+=" AND F_TABLE_SCHEMA = '" + schema + "'";
  }
  return readIntegerFromStatement(cx,metadataTableStatement);
}

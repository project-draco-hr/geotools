{
  boolean isValid=GpxUtils.isValidLatitudeValue(argLatitude);
  if (isValid == true) {
    this.latitude=argLatitude;
  }
 else {
    IllegalArgumentException toThrow=new IllegalArgumentException("The latitude value passed to the method was not valid.");
    throw toThrow;
  }
}

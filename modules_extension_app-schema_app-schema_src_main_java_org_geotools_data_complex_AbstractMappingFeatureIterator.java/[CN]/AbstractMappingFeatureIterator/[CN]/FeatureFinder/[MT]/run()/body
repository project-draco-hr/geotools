{
  try {
    Feature feature=DataAccessRegistry.getInstance().findFeature(new FeatureIdImpl(refId),hints,stopFlag);
    if (!stopFlag.get()) {
      setFeature(feature);
    }
  }
 catch (  IOException e) {
  }
}

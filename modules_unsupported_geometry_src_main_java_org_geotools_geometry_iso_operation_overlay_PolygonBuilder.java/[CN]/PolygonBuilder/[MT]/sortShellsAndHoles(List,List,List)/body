{
  for (Iterator it=edgeRings.iterator(); it.hasNext(); ) {
    EdgeRing er=(EdgeRing)it.next();
    if (er.isHole()) {
      freeHoleList.add(er);
    }
 else {
      shellList.add(er);
    }
  }
}

{
  MapLayer layer=event.getLayer();
  if (layerTable != null) {
    layerTable.onRemoveLayer(layer);
  }
  if (context.getLayerCount() == 0) {
    clearFields();
  }
 else {
    setFullExtent();
  }
  redraw();
}

{
  if (this.overlayImage != null)   this.overlayImage.dispose();
  this.overlayImage=overlayImage;
  this.overlayEnvelope=overlayEnvelope;
  this.overlayDoXor=overlayDoXor;
  if (boundsChanged) {
    redrawBaseImage=true;
  }
 else {
    redrawBaseImage=false;
  }
  redraw();
}

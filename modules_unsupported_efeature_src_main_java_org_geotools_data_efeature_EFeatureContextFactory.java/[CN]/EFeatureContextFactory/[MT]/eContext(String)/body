{
  EFeatureContext eContext=eContextMap.get(eContextID);
  if (eContext == null) {
    throw new IllegalArgumentException("EFeatureContext '" + eContextID + "' does not exist");
  }
  return eContext;
}

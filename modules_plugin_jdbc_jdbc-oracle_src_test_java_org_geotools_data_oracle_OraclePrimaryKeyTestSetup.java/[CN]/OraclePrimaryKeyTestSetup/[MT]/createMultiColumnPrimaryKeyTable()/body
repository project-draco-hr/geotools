{
  run("CREATE TABLE multi (key1 INT, key2 VARCHAR(255), name VARCHAR(255)," + " geom MDSYS.SDO_GEOMETRY, PRIMARY KEY(key1,key2))");
  run("INSERT INTO USER_SDO_GEOM_METADATA (TABLE_NAME, COLUMN_NAME, DIMINFO, SRID )" + " VALUES ('multi','geom',MDSYS.SDO_DIM_ARRAY(MDSYS.SDO_DIM_ELEMENT('X',-180,180,0.5), " + "MDSYS.SDO_DIM_ELEMENT('Y',-90,90,0.5)), 4326)");
  run("INSERT INTO multi VALUES (1,'x','one',NULL)");
  run("INSERT INTO multi VALUES (2,'y','two',NULL)");
  run("INSERT INTO multi VALUES (3,'z','three',NULL)");
}

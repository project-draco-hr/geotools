{
  try {
    return WRAPPERTYPE_FIXED_VALUE.equalsIgnoreCase(String.valueOf(WRAPPERTYPE.lookUp(params)));
  }
 catch (  Exception e) {
    LOGGER.finer("lookup failed on parameter " + WRAPPERTYPE.key);
    return false;
  }
}

{
  Duration oldHarvestInterval=harvestInterval;
  harvestInterval=newHarvestInterval;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,Csw20Package.HARVEST_TYPE__HARVEST_INTERVAL,oldHarvestInterval,harvestInterval));
}

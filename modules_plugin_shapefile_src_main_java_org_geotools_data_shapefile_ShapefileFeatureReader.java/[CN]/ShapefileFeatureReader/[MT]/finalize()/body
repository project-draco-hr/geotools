{
  if (shp != null) {
    LOGGER.log(Level.WARNING,"There is code leaving shapefile readers unclosed, " + "this might result in file system locks not being cleared. File is: " + schema.getTypeName());
    close();
  }
}

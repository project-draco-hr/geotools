{
  Object obj=toObjOrNull(expr);
  if (obj instanceof String && expr instanceof Literal) {
    String str=obj.toString();
    if (str.startsWith("#")) {
      obj=str.substring(1);
    }
  }
  return obj;
}

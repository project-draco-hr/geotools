{
  Object eNames=null;
  if (eHints != null) {
    eNames=eHints.get(EFeatureHints.EFEATURE_DEFAULT_GEOMETRY_NAMES);
  }
  return eNames != null ? (Set<String>)eNames : new HashSet<String>(Arrays.asList(EFeatureConstants.DEFAULT_GEOMETRY_NAME));
}

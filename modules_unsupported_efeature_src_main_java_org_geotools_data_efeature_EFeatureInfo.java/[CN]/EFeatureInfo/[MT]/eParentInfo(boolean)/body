{
  EFeaturePackageInfo eInfo=eContext(checkIsValid).eStructure().eGetPackageInfo(eNsURI);
  if (eInfo != null) {
    return eInfo.eGetFolderInfo(eFolderName);
  }
  return eContext(checkIsValid).eStructure();
}

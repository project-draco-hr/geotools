{
  Class<?> binding=type.getType().getBinding();
  for (int i=0; i < OGR_GEOM_TYPES.length; i++) {
    if (binding.equals(OGR_GEOM_TYPES[i]))     return i;
  }
  throw new IOException("Could not map " + binding + " to an OGR geometry type");
}

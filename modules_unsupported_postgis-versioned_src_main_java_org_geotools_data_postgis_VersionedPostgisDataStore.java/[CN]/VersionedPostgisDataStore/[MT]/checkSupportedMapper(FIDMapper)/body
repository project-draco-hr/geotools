{
  if (mapper instanceof TypedFIDMapper) {
    mapper=((TypedFIDMapper)mapper).getWrappedMapper();
  }
  for (int i=0; i < SUPPORTED_FID_MAPPERS.length; i++) {
    if (SUPPORTED_FID_MAPPERS[i].isAssignableFrom(mapper.getClass()))     return true;
  }
  return false;
}

{
  Iterator iter=files.values().iterator();
  VPFFile file=null;
  while (iter.hasNext()) {
    try {
      file=(VPFFile)iter.next();
      file.close();
    }
 catch (    Exception exc) {
      exc.printStackTrace();
    }
  }
  files.clear();
}

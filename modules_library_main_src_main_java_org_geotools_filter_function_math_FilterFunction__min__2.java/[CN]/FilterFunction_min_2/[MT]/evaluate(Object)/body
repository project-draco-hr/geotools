{
  long arg0;
  long arg1;
  try {
    arg0=((Number)getExpression(0).evaluate(feature)).longValue();
  }
 catch (  Exception e) {
    throw new IllegalArgumentException("Filter Function problem for function min argument #0 - expected type long");
  }
  try {
    arg1=((Number)getExpression(1).evaluate(feature)).longValue();
  }
 catch (  Exception e) {
    throw new IllegalArgumentException("Filter Function problem for function min argument #1 - expected type long");
  }
  return new Long(Math.min(arg0,arg1));
}

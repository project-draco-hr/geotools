{
  final StringBuffer buffer=new StringBuffer(owner.toString());
  buffer.append('[');
  for (int i=0; i < ordinates.length; i++) {
    if (i != 0)     buffer.append(", ");
    buffer.append(ordinates[i]);
  }
  buffer.append(']');
  return buffer.toString();
}

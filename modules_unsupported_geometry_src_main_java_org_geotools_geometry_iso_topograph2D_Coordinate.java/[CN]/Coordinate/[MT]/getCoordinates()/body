{
  double[] rCoord;
  if (Double.isNaN(this.z)) {
    rCoord=new double[2];
  }
 else {
    rCoord=new double[3];
  }
  rCoord[0]=this.x;
  rCoord[1]=this.y;
  if (!Double.isNaN(this.z)) {
    rCoord[2]=this.z;
  }
  return rCoord;
}

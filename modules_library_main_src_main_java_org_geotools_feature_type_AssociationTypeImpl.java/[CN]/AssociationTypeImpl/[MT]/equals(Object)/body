{
  if (!(other instanceof AssociationTypeImpl)) {
    return false;
  }
  AssociationType ass=(AssociationType)other;
  return super.equals(ass) && Utilities.equals(relatedType,ass.getRelatedType());
}

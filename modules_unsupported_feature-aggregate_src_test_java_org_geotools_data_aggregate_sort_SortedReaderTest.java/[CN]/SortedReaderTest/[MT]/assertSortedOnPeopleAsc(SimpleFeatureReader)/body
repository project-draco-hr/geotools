{
  double prev=-1;
  while (fr.hasNext()) {
    SimpleFeature f=fr.next();
    double curr=(Double)f.getAttribute("PERSONS");
    if (prev > 0) {
      assertTrue(curr >= prev);
    }
    prev=curr;
  }
}

{
  try {
    return new XmlComplexFeatureParser(getResourceAsFileInputStream(inputFileName),FakeTypes.Mine.MINETYPE_TYPE,FakeTypes.Mine.NAME_Mine);
  }
 catch (  IOException ioe) {
    Assert.fail("Can't find " + inputFileName);
  }
  return null;
}

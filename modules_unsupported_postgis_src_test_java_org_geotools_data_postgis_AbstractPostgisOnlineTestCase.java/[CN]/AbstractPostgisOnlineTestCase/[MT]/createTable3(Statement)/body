{
  String sql="CREATE TABLE " + table3 + "("+ "fid bigserial NOT NULL, "+ "name varchar(10), the_geom geometry, "+ "CONSTRAINT "+ table3+ "_pkey PRIMARY KEY (fid)"+ ") WITH OIDS;";
  st.execute(sql);
}

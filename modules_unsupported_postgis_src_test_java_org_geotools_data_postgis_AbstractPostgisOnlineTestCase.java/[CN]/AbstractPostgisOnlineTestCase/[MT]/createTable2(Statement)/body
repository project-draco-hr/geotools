{
  String sql="CREATE SEQUENCE " + table2 + "_fid_seq INCREMENT 1 MINVALUE 1 "+ "MAXVALUE 9223372036854775807 START 1001 CACHE 1;"+ "CREATE TABLE "+ table2+ "("+ "fid int4 NOT NULL DEFAULT nextval('"+ table2+ "_fid_seq'::text), name varchar(10), the_geom geometry, "+ "CONSTRAINT "+ table2+ "_pkey PRIMARY KEY (fid)"+ ") WITH OIDS;";
  st.execute(sql);
}

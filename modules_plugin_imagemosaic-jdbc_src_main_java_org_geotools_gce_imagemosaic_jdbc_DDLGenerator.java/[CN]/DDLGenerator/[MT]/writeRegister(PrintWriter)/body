{
  if (needsSpatialRegistry(config) == false) {
    return;
  }
  for (int i=0; i <= pyramids; i++) {
    w.print(dbDialect.getRegisterSpatialStatement(getTableName(spatialTNPrefix,i),srs));
    w.println(statementDelim);
  }
}

{
  if (bandsKeys == null || bandsKeys.isEmpty())   throw new IllegalArgumentException("Specified band keys list is invalid");
  init((String[])bandsKeys.toArray(new String[bandsKeys.size()]),name,description);
}

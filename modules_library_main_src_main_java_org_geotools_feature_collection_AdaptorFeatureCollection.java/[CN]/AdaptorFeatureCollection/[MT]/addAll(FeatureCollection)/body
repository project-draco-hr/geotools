{
  boolean modified=false;
  Iterator<SimpleFeature> e=c.iterator();
  try {
    while (e.hasNext()) {
      if (add(e.next()))       modified=true;
    }
  }
  finally {
    c.close(e);
  }
  return modified;
}

{
  List<Reference<?>> c=cache.get(category);
  if (c == null) {
    c=new LinkedList<Reference<?>>();
    cache.put(category,c);
  }
  @SuppressWarnings("unchecked") final List<Reference<T>> cheat=(List)c;
  return cheat;
}

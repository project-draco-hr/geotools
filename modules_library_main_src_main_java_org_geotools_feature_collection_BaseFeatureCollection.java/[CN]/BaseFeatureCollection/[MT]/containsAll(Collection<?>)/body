{
  SimpleFeatureIterator e=features();
  try {
    while (e.hasNext()) {
      SimpleFeature feature=e.next();
      if (!c.contains(feature)) {
        return false;
      }
    }
    return true;
  }
  finally {
    e.close();
  }
}

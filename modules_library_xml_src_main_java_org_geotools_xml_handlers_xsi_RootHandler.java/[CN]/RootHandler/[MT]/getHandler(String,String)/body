{
  if (SchemaHandler.LOCALNAME.equalsIgnoreCase(localName) && SchemaHandler.namespaceURI.equalsIgnoreCase(namespaceURI)) {
    if (schema == null) {
      schema=new SchemaHandler();
    }
    return schema;
  }
  logger.warning("Starting schema with " + localName + " element.");
  return null;
}

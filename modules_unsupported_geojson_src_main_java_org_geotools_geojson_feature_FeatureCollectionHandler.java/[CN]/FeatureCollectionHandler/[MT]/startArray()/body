{
  if (delegate == UNINITIALIZED) {
    delegate=new FeatureHandler(builder,attio);
    stack=new ArrayList();
    return true;
  }
  if (stack != null) {
    stack.add(null);
  }
  return super.startArray();
}

{
  if (!(object instanceof TypedFIDMapper)) {
    return false;
  }
  TypedFIDMapper other=(TypedFIDMapper)object;
  return other.wrappedMapper.equals(wrappedMapper) && (other.featureTypeName == featureTypeName);
}

{
  if (g == null) {
    ps.setNull(1,Types.BLOB);
  }
 else {
    try {
      ps.setBytes(column,new GeoPkgGeomWriter().write(g));
    }
 catch (    IOException e) {
      throw new RuntimeException(e);
    }
  }
}

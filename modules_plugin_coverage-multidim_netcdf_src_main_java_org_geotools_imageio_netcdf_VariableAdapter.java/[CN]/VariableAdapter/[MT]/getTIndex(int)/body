{
  if (rank > 2) {
    if (rank == 3) {
      return index;
    }
 else {
      return (int)Math.ceil(index / NetCDFUtilities.getDimensionLength(variableDS,zDimensionIndex));
    }
  }
  return -1;
}

{
  if (unset) {
    return null;
  }
  Stroke stroke=sf.createStroke(color,width,opacity,lineJoin,lineCap,dashArray,dashOffset,graphicFill.build(),this.graphicStroke.build());
  if (parent == null) {
    reset();
  }
  return stroke;
}

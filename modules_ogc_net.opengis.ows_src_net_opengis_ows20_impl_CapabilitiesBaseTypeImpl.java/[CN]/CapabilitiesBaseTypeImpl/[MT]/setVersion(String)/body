{
  String oldVersion=version;
  version=newVersion;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,Ows20Package.CAPABILITIES_BASE_TYPE__VERSION,oldVersion,version));
}

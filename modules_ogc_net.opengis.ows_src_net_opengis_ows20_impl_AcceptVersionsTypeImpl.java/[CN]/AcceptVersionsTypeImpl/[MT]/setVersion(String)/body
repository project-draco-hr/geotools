{
  String oldVersion=version;
  version=newVersion;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,Ows20Package.ACCEPT_VERSIONS_TYPE__VERSION,oldVersion,version));
}

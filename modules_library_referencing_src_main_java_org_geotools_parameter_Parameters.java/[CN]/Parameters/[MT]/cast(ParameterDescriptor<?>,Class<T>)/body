{
  if (descriptor != null) {
    final Class<?> actual=descriptor.getValueClass();
    if (!type.equals(actual)) {
      throw new ClassCastException(Errors.format(ErrorKeys.BAD_PARAMETER_TYPE_$2,descriptor.getName().getCode(),actual));
    }
  }
  return (ParameterDescriptor)descriptor;
}

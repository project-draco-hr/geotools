{
  verify();
  String oldSRID=getSRID();
  if (newSRID != oldSRID) {
    getStructure().setSRID(newSRID);
    if (eImpl().eNotificationRequired())     eImpl().eNotify(new ENotificationImpl(eImpl(),Notification.SET,EFeaturePackage.EFEATURE__SRID,oldSRID,newSRID));
  }
}

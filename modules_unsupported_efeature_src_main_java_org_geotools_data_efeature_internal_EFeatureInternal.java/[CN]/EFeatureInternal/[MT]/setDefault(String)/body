{
  verify();
  String oldDefault=getDefault();
  getStructure().eSetDefaultGeometryName(newDefault);
  if (eImpl().eNotificationRequired())   eImpl().eNotify(new ENotificationImpl(eImpl(),Notification.SET,EFeaturePackage.EFEATURE__DEFAULT,oldDefault,newDefault));
}

{
  String oldVersion=version;
  version=newVersion;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,Csw20Package.TRANSACTION_RESPONSE_TYPE__VERSION,oldVersion,version));
}

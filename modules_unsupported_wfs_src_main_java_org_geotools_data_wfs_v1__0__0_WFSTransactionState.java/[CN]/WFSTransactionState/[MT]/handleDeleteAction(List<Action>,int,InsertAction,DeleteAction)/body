{
  if (deleteAction.getFilter().evaluate(action.getFeature())) {
    actions.remove(i);
    if (deleteAction.getFilter() instanceof Id && ((Id)deleteAction.getFilter()).getIdentifiers().size() == 1) {
      actions.remove(i);
    }
  }
 else {
    swap(actions,i);
  }
}

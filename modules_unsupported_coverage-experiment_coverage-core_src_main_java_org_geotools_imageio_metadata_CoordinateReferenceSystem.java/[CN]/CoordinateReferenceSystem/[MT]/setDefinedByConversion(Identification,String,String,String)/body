{
  if (!isProjectedOrDerived)   throw new IllegalArgumentException("Could not set definedByConversion parameters since this CoordinateReferenceSystem is not a Derived/Projected CoordinateReferenceSystem");
  definedByConversion.setDefinedByConversion(identification,formula,srcDim,targetDim);
}

{
  if (!isProjectedOrDerived)   throw new IllegalArgumentException("Could not set BaseCRS since this CoordinateReferenceSystem is not a Derived/Projected CoordinateReferenceSystem");
  baseCRS.setIdentification(identification);
}

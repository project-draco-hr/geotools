{
  if (!isProjectedOrDerived) {
    super.addEllipsoid(semiMajorAxis,semiMinorAxis,invFlattening,unit,ellipsoidId);
  }
 else   baseCRS.addEllipsoid(semiMajorAxis,semiMinorAxis,invFlattening,unit,ellipsoidId);
}

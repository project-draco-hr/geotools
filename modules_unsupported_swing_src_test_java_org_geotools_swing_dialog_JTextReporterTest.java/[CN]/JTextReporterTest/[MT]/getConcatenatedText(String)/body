{
  StringBuilder sb=new StringBuilder();
  for (int i=0; i < TEXT.length; i++) {
    sb.append(TEXT[i]);
    if (i < TEXT.length - 1) {
      sb.append(concatStr);
    }
  }
  return sb.toString();
}

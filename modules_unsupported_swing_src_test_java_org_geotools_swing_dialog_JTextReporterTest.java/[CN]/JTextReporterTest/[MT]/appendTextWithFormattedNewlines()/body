{
  String text=String.format("That's not what motivates me %n" + "Enough of this petty stuff for me %n" + "Not interested in pedantry %n"+ "I'm interested in apathy");
  Connection conn=showDialog(TITLE).get();
  conn.append(text);
  windowFixture.robot.waitForIdle();
  String displayedText=windowFixture.textBox().text();
  assertEquals(text,displayedText);
}

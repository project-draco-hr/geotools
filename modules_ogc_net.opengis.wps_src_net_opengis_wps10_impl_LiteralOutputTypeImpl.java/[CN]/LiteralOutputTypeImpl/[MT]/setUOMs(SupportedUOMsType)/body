{
  if (newUOMs != uOMs) {
    NotificationChain msgs=null;
    if (uOMs != null)     msgs=((InternalEObject)uOMs).eInverseRemove(this,EOPPOSITE_FEATURE_BASE - Wps10Package.LITERAL_OUTPUT_TYPE__UO_MS,null,msgs);
    if (newUOMs != null)     msgs=((InternalEObject)newUOMs).eInverseAdd(this,EOPPOSITE_FEATURE_BASE - Wps10Package.LITERAL_OUTPUT_TYPE__UO_MS,null,msgs);
    msgs=basicSetUOMs(newUOMs,msgs);
    if (msgs != null)     msgs.dispatch();
  }
 else   if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,Wps10Package.LITERAL_OUTPUT_TYPE__UO_MS,newUOMs,newUOMs));
}

{
  ArrayList<F> result=new ArrayList<F>();
  FeatureIterator<F> fi=null;
  try {
    fi=features();
    for (int i=0; fi.hasNext(); i++) {
      result.add(fi.next());
    }
    return result;
  }
  finally {
    close(fi);
  }
}

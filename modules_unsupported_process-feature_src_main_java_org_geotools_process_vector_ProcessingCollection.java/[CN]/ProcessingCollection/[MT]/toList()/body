{
  ArrayList<F> result=new ArrayList<F>();
  FeatureIterator<F> fi=null;
  try {
    fi=features();
    while (fi.hasNext()) {
      result.add(fi.next());
    }
    return result;
  }
  finally {
    if (fi != null) {
      fi.close();
    }
  }
}

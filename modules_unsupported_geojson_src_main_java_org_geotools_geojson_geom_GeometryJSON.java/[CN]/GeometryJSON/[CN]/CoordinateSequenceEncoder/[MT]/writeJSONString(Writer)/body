{
  int size=seq.size();
  if (size > 1) {
    out.write("[");
  }
  for (int i=0; i < seq.size(); i++) {
    out.write("[");
    out.write(String.valueOf(seq.getX(i)));
    out.write(",");
    out.write(String.valueOf(seq.getY(i)));
    out.write("]");
    if (i < seq.size() - 1) {
      out.write(",");
    }
  }
  if (size > 1) {
    out.write("]");
  }
}

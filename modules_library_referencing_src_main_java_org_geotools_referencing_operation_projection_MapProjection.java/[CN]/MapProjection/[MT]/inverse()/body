{
  if (!invertible) {
    throw new NoninvertibleTransformException(Errors.format(ErrorKeys.NONINVERTIBLE_TRANSFORM));
  }
  if (inverse == null) {
    inverse=new Inverse();
  }
  return inverse;
}

{
  String oldFormatSupported=formatSupported;
  formatSupported=newFormatSupported;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,Wcs20Package.SERVICE_METADATA_TYPE__FORMAT_SUPPORTED,oldFormatSupported,formatSupported));
}

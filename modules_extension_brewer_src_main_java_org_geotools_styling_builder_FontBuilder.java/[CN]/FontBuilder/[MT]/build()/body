{
  if (unset) {
    return null;
  }
  if (families.isEmpty()) {
    families.add(FF.literal("Serif"));
  }
  Font font=sf.font(families,style,weight,size);
  if (parent == null) {
    reset();
  }
  return font;
}

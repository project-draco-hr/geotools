{
  if (datum.getSelectedChoice() == Datum.IMAGE_DATUM)   return datum.getChild().getString(SpatioTemporalMetadataFormat.MD_DTM_ID_PIXELINCELL);
 else   throw new IllegalArgumentException("Could not get Pixel in Cell for non-Image Datum type");
}

{
  DataSource ds=ogr.OpenShared(ogrName,OGRDataStore.FALSE);
  if (ds != null) {
    ds.delete();
    return true;
  }
  Driver dr=ogr.GetDriverByName(driver);
  if (dr != null) {
    dr.delete();
    return true;
  }
  return false;
}

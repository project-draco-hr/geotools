{
  if (e != null && e.getKeyCode() == keyCode) {
    int emod=e.getModifiersEx();
    if (modifiers == 0) {
      return emod == 0;
    }
 else {
      return (emod & modifiers) == modifiers;
    }
  }
  return false;
}

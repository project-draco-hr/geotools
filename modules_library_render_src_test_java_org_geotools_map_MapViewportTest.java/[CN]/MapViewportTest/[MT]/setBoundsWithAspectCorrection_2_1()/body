{
  double w=WORLD.getWidth();
  ReferencedEnvelope expected=new ReferencedEnvelope(WORLD.getMinX() - w / 2,WORLD.getMaxX() + w / 2,WORLD.getMinY(),WORLD.getMaxY(),WORLD.getCoordinateReferenceSystem());
  assertAspectCorrection(SCREEN_2_1,expected);
}

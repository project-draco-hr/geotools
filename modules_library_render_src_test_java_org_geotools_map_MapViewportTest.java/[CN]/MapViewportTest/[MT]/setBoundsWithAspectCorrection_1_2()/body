{
  double h=WORLD.getHeight();
  ReferencedEnvelope expected=new ReferencedEnvelope(WORLD.getMinX(),WORLD.getMaxX(),WORLD.getMinY() - h / 2,WORLD.getMaxY() + h / 2,WORLD.getCoordinateReferenceSystem());
  assertAspectCorrection(SCREEN_1_2,expected);
}

{
  StringBuilder sb=new StringBuilder();
  for (int i=0; i < values.size(); i++) {
    sb.append(values.get(i).toLiteral());
    if (i < values.size() - 1) {
      sb.append(" ");
    }
  }
  return sb.toString();
}

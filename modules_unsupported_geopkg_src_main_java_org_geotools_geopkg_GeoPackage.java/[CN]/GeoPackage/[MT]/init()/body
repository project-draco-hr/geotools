{
  try {
    Connection cx=connPool.getConnection();
    try {
      init(cx);
      initialised=true;
    }
  finally {
      close(cx);
    }
  }
 catch (  SQLException e) {
    throw new IOException(e);
  }
}

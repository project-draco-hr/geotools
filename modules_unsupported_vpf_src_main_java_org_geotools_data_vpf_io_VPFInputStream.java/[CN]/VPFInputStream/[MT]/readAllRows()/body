{
  LinkedList list=new LinkedList();
  try {
    setPosition(1);
  }
 catch (  IOException exc) {
    return list;
  }
  VPFRow row=readRow();
  while (row != null) {
    list.add(row);
    row=readRow();
  }
  return list;
}

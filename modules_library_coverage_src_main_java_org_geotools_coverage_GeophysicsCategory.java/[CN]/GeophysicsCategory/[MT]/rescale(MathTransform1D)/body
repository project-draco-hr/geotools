{
  if (sampleToGeophysics.isIdentity()) {
    return this;
  }
  sampleToGeophysics=(MathTransform1D)ConcatenatedTransform.create(inverse.getSampleToGeophysics(),sampleToGeophysics);
  return inverse.rescale(sampleToGeophysics).inverse;
}

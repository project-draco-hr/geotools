{
  node.data.clear();
  for (int i=0; i < 10; i++) {
    GridData gd1=new GridData(mbr,data + i);
    GridData gd2=new GridData(mbr,data2 + i);
    node.insertData(gd1);
    node.insertData(gd2);
  }
}

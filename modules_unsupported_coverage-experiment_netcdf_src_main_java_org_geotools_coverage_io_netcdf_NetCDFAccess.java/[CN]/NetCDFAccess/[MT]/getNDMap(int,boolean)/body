{
  if (dim == 2)   return Coverages2DMap;
  if (dim == 3)   if (hasVertical)   return Coverages3DZMap;
 else   return Coverages3DTMap;
  if (dim == 4)   return Coverages4DMap;
  throw new IllegalArgumentException(" Unable to find a proper ND Coverage map");
}

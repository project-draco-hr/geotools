{
  if (coord.length != this.getDimension()) {
    throw new IllegalArgumentException("Index " + coord.length + " out of coordinate range (expected "+ getDimension()+ ")");
  }
  this.coordinate=coord;
}

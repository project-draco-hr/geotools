{
  if (dimension >= this.coordinate.length || dimension < 0) {
    throw new IndexOutOfBoundsException("Index " + dimension + " out of coordinate range (max "+ coordinate.length+ ")");
  }
  this.coordinate[dimension]=value;
}

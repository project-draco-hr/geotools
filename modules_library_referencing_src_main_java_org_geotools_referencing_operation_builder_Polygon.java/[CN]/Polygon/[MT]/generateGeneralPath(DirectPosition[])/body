{
  GeneralPath ring=new GeneralPath();
  ring.moveTo((float)points[0].getCoordinates()[0],(float)points[0].getCoordinates()[1]);
  for (int i=1; i < points.length; i++) {
    ring.lineTo((float)points[i].getCoordinates()[0],(float)points[i].getCoordinates()[1]);
  }
  return ring;
}

{
  for (int k=0; k < ft.getAttributeCount() && j == -1; k++) {
    if ((ft.getDescriptor(k).getLocalName() == null && value[i].getElement().getName() == null) || ft.getDescriptor(k).getLocalName().equals(value[i].getElement().getName()))     j=k;
  }
  return j;
}

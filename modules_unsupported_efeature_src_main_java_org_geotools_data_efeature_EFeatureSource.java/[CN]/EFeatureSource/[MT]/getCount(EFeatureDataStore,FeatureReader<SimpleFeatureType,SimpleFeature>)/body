{
  int count=0;
  eDataStore.getLogger().fine("Calculating feature count manually");
  try {
    while (eReader.hasNext()) {
      eReader.next();
      count++;
    }
  }
  finally {
    eReader.close();
  }
  return count;
}

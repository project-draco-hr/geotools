{
  StringBuilder builder=new StringBuilder();
  for (  String it : eFolders) {
    if (builder.length() > 0) {
      builder.append("+");
    }
    builder.append(it);
  }
  return builder.toString();
}

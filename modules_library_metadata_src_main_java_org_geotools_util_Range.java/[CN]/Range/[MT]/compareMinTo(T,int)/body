{
  if (value == null) {
    return delta;
  }
  if (minValue == null) {
    return -1;
  }
  final int c=minValue.compareTo(value);
  if (c == 0) {
    if (isMinIncluded) {
      return -delta;
    }
    if (delta <= 0) {
      return +1;
    }
  }
  return c;
}

{
  try {
    out.write("NOT (");
    filter.getFilter().accept(this,extraData);
    out.write(")");
    return extraData;
  }
 catch (  IOException e) {
    throw new RuntimeException(IO_ERROR,e);
  }
}

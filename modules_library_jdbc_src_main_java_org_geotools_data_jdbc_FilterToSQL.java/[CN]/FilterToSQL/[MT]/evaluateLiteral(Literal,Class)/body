{
  Object literal=null;
  if (target != null && !(Number.class.isAssignableFrom(target)))   literal=expression.evaluate(null,target);
  if (literal == null)   literal=expression.evaluate(null);
  if (literal == null)   literal=expression.getValue();
  return literal;
}

{
  SolrDataStore dataStore=(SolrDataStore)dataStoreFactory.createDataStore((Map)new KVP(URL.key,"http://localhost:8080/solr/geotools",LAYER_MAPPER.key,Type.SINGLE.name()));
  assertTrue(dataStore.getLayerMapper() instanceof SingleLayerMapper);
}

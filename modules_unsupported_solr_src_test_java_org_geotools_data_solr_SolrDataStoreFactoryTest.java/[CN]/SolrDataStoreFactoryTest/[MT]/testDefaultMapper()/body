{
  SolrDataStore dataStore=(SolrDataStore)dataStoreFactory.createDataStore((Map)new KVP(URL.key,"http://localhost:8080/solr/geotools",FIELD.key,"foo"));
  assertTrue(dataStore.getLayerMapper() instanceof FieldLayerMapper);
}

{
  SimpleFeatureIterator fi=null;
  try {
    int count=0;
    fi=features();
    while (fi.hasNext()) {
      fi.next();
      count++;
    }
    return count;
  }
  finally {
    fi.close();
  }
}

{
  try (BufferedReader reader=new BufferedReader(new InputStreamReader(script));Connection conn=getConnection();Statement st=conn.createStatement()){
    String line=null;
    while ((line=reader.readLine()) != null) {
      LOGGER.fine(line);
      st.execute(line);
    }
  }
 }

{
  if (toRemove != null) {
    if (trash != null) {
      toRemove.setValue(trash);
    }
 else {
      iterator.remove();
    }
    toRemove=null;
  }
 else {
    throw new IllegalStateException();
  }
}

{
  if (foreignIds != null) {
    for (int i=0; i < foreignIds.size(); i++) {
      if (!peekValue(next,foreignIds.get(i)).equals(foreignIdValues.get(i))) {
        return false;
      }
    }
  }
  return true;
}

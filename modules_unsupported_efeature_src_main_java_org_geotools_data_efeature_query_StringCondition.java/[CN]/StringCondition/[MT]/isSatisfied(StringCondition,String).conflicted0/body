{
  return (cond.lowerInclusive ? (cond.lowerBound.compareTo(str) >= 0) : (cond.lowerBound.compareTo(str) > 0)) || (cond.upperInclusive ? (cond.upperBound.compareTo(str) <= 0) : (cond.upperBound.compareTo(str) < 0));
}

{
  if (responseStream != null) {
    try {
      responseStream.close();
    }
 catch (    IOException e) {
    }
    responseStream=null;
  }
  if (connection != null) {
    connection.disconnect();
    connection=null;
  }
}

{
  LineString ls=null;
  if (obj instanceof MultiLineString)   ls=(LineString)((MultiLineString)obj).getGeometryN(0);
 else   ls=(LineString)obj;
  Edge e=(Edge)super.add(new LineSegment(ls.getCoordinateN(0),ls.getCoordinateN(ls.getNumPoints() - 1)));
  e.setObject(ls);
  return (e);
}

{
  try {
    if (dataSource != null) {
      dataSource.close();
    }
    super.tearDown();
  }
  finally {
    System.clearProperty(Context.INITIAL_CONTEXT_FACTORY);
    GeoTools.clearInitialContext();
  }
}

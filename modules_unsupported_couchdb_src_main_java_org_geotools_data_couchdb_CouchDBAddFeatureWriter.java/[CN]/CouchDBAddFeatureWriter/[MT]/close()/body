{
  try {
    if (wrote > 0) {
      buffer.write("]}");
      String data=buffer.toString();
      dataStore.getConnection().postBulk(data);
    }
  }
 catch (  CouchDBException ex) {
    ex.wrap();
  }
}

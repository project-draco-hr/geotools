{
  String sql="CREATE TABLE " + TEST_SCHEMA + "."+ table7+ "("+ "fid serial NOT NULL,"+ "name varchar(10), the_geom geometry, "+ "CONSTRAINT "+ table7+ "_pkey PRIMARY KEY (fid)"+ ") WITH OIDS;";
  st.execute(sql);
}

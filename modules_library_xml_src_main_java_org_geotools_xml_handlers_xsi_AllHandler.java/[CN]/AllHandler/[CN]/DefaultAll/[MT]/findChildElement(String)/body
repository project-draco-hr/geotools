{
  if (elements == null) {
    return null;
  }
  for (int i=0; i < elements.length; i++) {
    Element t=elements[i].findChildElement(name);
    if (t != null) {
      return t;
    }
  }
  return null;
}

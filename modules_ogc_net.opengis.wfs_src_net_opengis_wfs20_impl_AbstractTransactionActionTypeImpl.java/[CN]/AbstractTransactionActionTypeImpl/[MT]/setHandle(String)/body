{
  String oldHandle=handle;
  handle=newHandle;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,Wfs20Package.ABSTRACT_TRANSACTION_ACTION_TYPE__HANDLE,oldHandle,handle));
}

{
  if (object == this) {
    return true;
  }
  if (super.equals(object)) {
    final LambertConformal that=(LambertConformal)object;
    return (this.belgium == that.belgium) && equals(this.n,that.n) && equals(this.F,that.F)&& equals(this.rho0,that.rho0)&& equals(this.phi1,that.phi1)&& equals(this.phi2,that.phi2);
  }
  return false;
}

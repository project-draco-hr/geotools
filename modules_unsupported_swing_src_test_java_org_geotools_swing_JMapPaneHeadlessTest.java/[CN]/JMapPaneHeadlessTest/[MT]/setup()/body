{
  mapPane=GuiActionRunner.execute(new GuiQuery<JMapPane>(){
    @Override protected JMapPane executeInEDT() throws Throwable {
      return new JMapPane();
    }
  }
);
  listener=new WaitingMapPaneListener();
}

{
  final Raster[] pendings=this.pendings;
  if (pendings != null) {
    final int length=pendings.length;
    for (int i=0; i < length; i++) {
      if (pendings[i] != null) {
        return true;
      }
    }
  }
  return false;
}

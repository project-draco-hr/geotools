{
  String pattern=null;
  if (geom != null)   pattern=PARAMETER_MARKES.get(geom.getClass());
  if (pattern == null)   pattern=PARAMETER_MARKES.get(binding);
  sql.append(MessageFormat.format(pattern,new Object[]{Integer.toString(srid)}));
}

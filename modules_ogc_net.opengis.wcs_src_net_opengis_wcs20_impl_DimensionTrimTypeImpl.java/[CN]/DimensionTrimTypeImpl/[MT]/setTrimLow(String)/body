{
  String oldTrimLow=trimLow;
  trimLow=newTrimLow;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,Wcs20Package.DIMENSION_TRIM_TYPE__TRIM_LOW,oldTrimLow,trimLow));
}

{
  initialize();
  checkImageIndex(imageIndex);
  if (formatName.equalsIgnoreCase(HDF4TeraScanImageMetadata.nativeMetadataFormatName))   return new HDF4TeraScanImageMetadata(this,imageIndex);
  return super.getImageMetadata(imageIndex,formatName,nodeNames);
}

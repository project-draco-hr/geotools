{
  int code=0;
  if ((fieldsArr == null) || (fieldsArr.length == 0)) {
    code=super.hashCode();
  }
 else {
    for (int i=0; i < fieldsArr.length; i++) {
      code+=fieldsArr[i].hashCode();
    }
  }
  return code;
}

{
  this(contentState);
  if (append) {
    this.appending=true;
    while (this.hasNext()) {
      this.each=this.next();
      this.write();
    }
    this.appending=false;
  }
  this.append=append;
}

{
  Style[] style=styles(sld);
  for (int i=0; i < style.length; i++) {
    if (style[i].isDefault()) {
      return style[i];
    }
  }
  if (style.length == 0) {
    return null;
  }
  return style[0];
}

{
  final double zoneCount=abs(360 / zoneWidth);
  double t;
  t=centralLongitudeZone1 - 0.5 * zoneWidth;
  t=toDegrees(centralMeridian) - t;
  t=floor(t / zoneWidth + EPSILON);
  t-=zoneCount * floor(t / zoneCount);
  return ((int)t) + 1;
}

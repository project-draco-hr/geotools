{
synchronized (this) {
    if (r == lr && g == lg && b == lb && a == la && idx >= 0) {
      return idx;
    }
  }
  int delegateIdx=delegate.getClosestIndex(r,g,b,a);
synchronized (this) {
    lr=r;
    lg=g;
    lb=b;
    la=a;
    idx=delegateIdx;
  }
  return delegateIdx;
}

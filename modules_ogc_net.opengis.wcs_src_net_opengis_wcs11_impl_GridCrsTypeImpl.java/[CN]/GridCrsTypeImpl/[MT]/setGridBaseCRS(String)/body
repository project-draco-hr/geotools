{
  String oldGridBaseCRS=gridBaseCRS;
  gridBaseCRS=newGridBaseCRS;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,Wcs111Package.GRID_CRS_TYPE__GRID_BASE_CRS,oldGridBaseCRS,gridBaseCRS));
}

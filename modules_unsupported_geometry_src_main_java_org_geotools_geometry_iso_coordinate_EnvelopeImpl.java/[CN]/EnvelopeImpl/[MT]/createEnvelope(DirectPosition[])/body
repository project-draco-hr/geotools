{
  if (p.length == 0) {
    return null;
  }
  EnvelopeImpl result=new EnvelopeImpl(p[0]);
  for (int i=1; i < p.length; ++i) {
    if (p[i] != null) {
      result.add(p[i]);
    }
  }
  return result;
}

{
  if (!PARAM_NAMES.contains(key)) {
    LOGGER.log(Level.WARNING,"Adding unexpected parameter {0} to {1} Contrast Enhancer",new Object[]{key,NAME});
  }
  if (params == null)   params=new HashMap<String,Expression>();
  params.put(key,value);
}

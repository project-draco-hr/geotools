{
  String rString="";
  if (pa.size() == 0)   return "";
  for (int i=1; i < pa.size(); i++) {
    if (i > 1) {
      rString+=", ";
    }
    rString+=directPositionToString(pa.get(i).getDirectPosition());
  }
  return rString;
}

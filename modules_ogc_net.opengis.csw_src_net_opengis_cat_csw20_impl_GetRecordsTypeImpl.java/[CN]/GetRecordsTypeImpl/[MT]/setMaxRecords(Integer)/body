{
  Integer oldMaxRecords=maxRecords;
  maxRecords=newMaxRecords;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,Csw20Package.GET_RECORDS_TYPE__MAX_RECORDS,oldMaxRecords,maxRecords));
}

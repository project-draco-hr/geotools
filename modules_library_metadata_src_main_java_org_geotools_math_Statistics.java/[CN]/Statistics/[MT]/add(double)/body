{
  if (!Double.isNaN(sample)) {
    if (!(min <= sample))     min=sample;
    if (!(max >= sample))     max=sample;
    sum2+=(sample * sample);
    sum+=sample;
    n++;
  }
 else {
    nNaN++;
  }
}

{
  ensureNonNull("range",range);
  final Class<?> type=range.elementClass;
  ensureNumberClass(type);
  @SuppressWarnings("unchecked") final Class<? extends Number> result=(Class<? extends Number>)type;
  return result;
}

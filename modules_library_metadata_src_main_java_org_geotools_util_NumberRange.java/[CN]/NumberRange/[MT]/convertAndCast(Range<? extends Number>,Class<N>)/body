{
  if (type.equals(range.getElementClass())) {
    @SuppressWarnings("unchecked") final NumberRange<N> cast=(NumberRange<N>)wrap((Range)range);
    return cast;
  }
  return new NumberRange<N>(type,(Range)range);
}

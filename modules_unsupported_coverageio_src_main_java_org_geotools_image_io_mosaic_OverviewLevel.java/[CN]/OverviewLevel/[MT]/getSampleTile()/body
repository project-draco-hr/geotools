{
  if (sample == null) {
    if (patterns != null) {
      int i=0;
      do {
        sample=patterns[i++];
      }
 while (sample == null);
    }
 else {
      int i=0;
      do {
        sample=tiles.get(i++);
      }
 while (sample == null);
    }
  }
  return sample;
}

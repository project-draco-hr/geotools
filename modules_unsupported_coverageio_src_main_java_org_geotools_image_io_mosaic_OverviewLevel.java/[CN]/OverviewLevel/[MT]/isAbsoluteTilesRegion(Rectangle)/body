{
  final int width=dx * xSubsampling;
  if (bounds.width % width == 0) {
    final int height=dy * ySubsampling;
    if (bounds.height % height == 0) {
      return (bounds.x - xOffset * xSubsampling) % width == 0 && (bounds.y - yOffset * ySubsampling) % height == 0;
    }
  }
  return false;
}

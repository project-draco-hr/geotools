{
  if (cached == null) {
    ISession session=super.getSession(transactional);
    this.cached=new SessionWrapper(session){
      @Override public void dispose(){
      }
    }
;
  }
  return cached;
}

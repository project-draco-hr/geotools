{
  if (code.equals("EPSG:" + DISPLAY_CODE)) {
    return new SimpleInternationalString("A two-dimensional wildcard coordinate system with X,Y axis where Y displays dows");
  }
 else {
    throw noSuchAuthorityException(code);
  }
}

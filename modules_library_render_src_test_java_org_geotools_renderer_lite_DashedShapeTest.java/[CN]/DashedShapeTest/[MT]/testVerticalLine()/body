{
  Line2D line=new Line2D.Double(0,0,0,30);
  Shape stroked=new DashedShape(line,new float[]{10,10},0);
  checkExpected(stroked,new float[]{0,0,0},new float[]{1,0,10},new float[]{0,0,20},new float[]{1,0,30});
}

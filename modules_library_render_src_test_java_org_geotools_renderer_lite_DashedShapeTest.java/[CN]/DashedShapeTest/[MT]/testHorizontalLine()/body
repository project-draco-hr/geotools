{
  Line2D line=new Line2D.Double(0,0,30,0);
  Shape stroked=new DashedShape(line,new float[]{10,10},0);
  checkExpected(stroked,new float[]{0,0,0},new float[]{1,10,0},new float[]{0,20,0},new float[]{1,30,0});
}
